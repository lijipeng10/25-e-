# ============================================================================
#  .gitignore —— 智能车 / MSPM0G3507 (CCS Theia)
# ============================================================================

# CCS 构建输出目录
#   里面的 .o/.d/.map/.out/makefile 全部可由 empty.syscfg + 源码重新生成,
#   连 ti_msp_dl_config.c/.h 也是 SysConfig 生成的, 所以整个目录不入库。
#   在 CCS 里重新构建工程即可全部恢复。
Debug/

# Theia/VS Code 本地状态(窗口布局、最近打开等), 与工程无关
.theia/

# 本机专用的 AI 技能目录(DSH 会从这里发现 skill)
#   只在本机用, 换了机器/换个人拉下来不需要它, 所以不入库。
#   需要的话把对应 skill 目录拷到 <工程>/.dsh/skills/ 即可。
.dsh/
.agents/

# 编译中间产物(万一散落在仓库别处)
*.o
*.d
*.d_raw
*.map
*.out
*.hex
*.bin

# 备份/临时文件
*~
*.bak
*.tmp
*.log
