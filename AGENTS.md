# AGENTS.md —— 本工程开发约定

> 本文件给 AI 助手(以及人)看: 在这个仓库里干活必须遵守的规则。

---

## 注意事项

- **每次改动完成后，都必须创建一个对应的 Git commit**，以便后续追踪和回滚。
- **每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。**

---

## 本工程怎么落地上面的两条

### 1. 每次改动都要 commit

- 一个逻辑改动 = 一个 commit，不要攒着一起提交。
- commit message 写清楚**改了什么、为什么改**；踩过的坑一并写进去（这类信息最值钱）。
- 提交前确认 `git status` 里没有误加入的构建产物 —— `Debug/` 已被 `.gitignore` 排除。

### 2. 每次改动都要测试/验证

本工程是嵌入式裸机工程，没有单元测试框架，所以"测试"分两层，**两层都要做**：

**(a) 编译验证（每次必做，可自动）**

```powershell
& 'D:/TI/ccs2025/ccs/utils/bin/gmake.exe' -C Debug -j4 all
```

验收标准：**exit code 0，且没有 error、没有 warning**。
注意：`gmake` 的报错会带 `[exit code: N]` 标记；只看"Finished building target"
不够，必须确认没有 warning。

**(b) 硬件验证（涉及外设行为时必须做）**

编译通过只能保证语法正确，**不能保证硬件行为正确**。
凡是改了外设初始化、时序、引脚、通信协议，交付前都要给出**可观察的验收现象**：

| 改了什么 | 需要的验收现象 |
| --- | --- |
| 显示(OLED) | 屏幕上出现预期画面，字符可读、边框完整 |
| 电机 | 按指定按键后电机按预期方向/速度转动 |
| 按键 | 串口或屏幕能看到正确的键码 |
| 通信(SPI/I2C/UART) | 对端能正确收到数据 |
| 传感器(灰度/陀螺仪) | 串口或屏幕能看到合理数值 |

如果无法自行烧录验证，**必须在交付时明确告诉用户**：
"编译已通过，但硬件行为需要你烧录确认，验收现象是 XXX"。
**不要把"能编译"当作"功能正确"交付。**

### 3. 交付时用「状态阶梯」说话，别用模糊的话

每一步改动都要说清它走到哪一级：

```
Planned -> Implemented -> Build Passed -> Host Verified -> HW Verified -> Accepted
```

| 状态 | 含义 |
| --- | --- |
| `Planned` | 只写了方案，还没动代码 |
| `Implemented` | 代码改完了 |
| `Build Passed` | 编译 0 error 0 warning。**不代表功能对** |
| `Host Verified` | 在电脑上验证过（脚本、串口解析、数据核对等） |
| `HW Verified` | **在真实硬件上观察到预期现象**（用户烧录确认，或我能直接观测） |
| `Accepted` | 验收条件**逐条**都有证据对上 |

交付时必须说明：

- 改了哪些文件
- **现在到哪一级**（我给出的多数结论只到 `Build Passed`）
- 到 `HW Verified` 还差什么、**验收现象具体是什么**

---

## 改动纪律（风险分级 / 证据 / 最小实现 / 收尾）

> 这一节的思路借鉴了 `embedded-project-governance`（本机 `.dsh/skills/` 下那份）。
> **本工程不直接调用那个 skill**，只把有用的纪律固化在这里。

### 风险分级：高风险改动，动手前先停一下

| 级别 | 什么算 | 怎么做 |
| --- | --- | --- |
| 低 | 局部、可逆，不碰共享接口 / 持久化 / 并发 / 硬件状态 | 直接改，做最小检查 |
| 中 | 共享接口、通信协议、RTOS 交互、持久化、资源预算、兼容性 | 先写清目标 / 范围 / 验收 |
| **高** | **启动与复位、时钟、Flash/NVM、看门狗、DMA 归属、中断优先级、电源与执行器输出、任何不可逆操作** | **先冻结「影响范围 + 回滚方式 + 验收方法」，再动手** |

本工程踩过的坑基本都落在「高」这一档：

- **改时钟源**（HFXT→SYSOSC）：影响**所有**外设频率，必须逐个核对
  （PWM 分频、SPI 位率、定时器预分频、延时/时基基准）
- **改 `empty.syscfg` 的引脚**：SysConfig 生成失败会让编译**中途停住**，
  结果是「改了代码但板子上还是旧固件」
- **改电机 PWM 周期**：必须同步 `hardware/motor.c` 的 `MOTOR_PWM_PERIOD`

### 证据规则

- 编译证据、主机证据、硬件证据是**三道独立的门**，不能互相替代。
- **一次成功 ≠ 边界正确、≠ 恢复正确。** 只跑通一次不算验证 ——
  要覆盖边界、超时、异常和恢复路径。
- 交付时把证据绑到**可复现的身份**上：commit hash + 观测方法 + 实际现象。
- 警告、stub、`TODO`、跳过的检查，一律算**未完成**，除非明确接受残余风险。
- 重要失败之后，确认系统处于**可重试 / 安全 / 明确终止**的状态，别留半死不活的现场。

### 最小实现：从最轻的一级开始，够用就停

1. 不做没证据支持的猜测性工作
2. **先复用**本工程已有的驱动 / 工具 / 状态机 / 模式（先查 `PROJECT.md` 的能力地图）
3. 再用 SDK / HAL / CMSIS
4. 再考虑合适的硬件外设
5. 最后才是**一处小改动**（一个函数 / 一张表 / 一个状态迁移 / 一个配置）
6. 只有证据要求时才继续加

**不要**在没实证需求的情况下加：新的任务 / 队列 / 锁、堆分配、通用抽象层、
功能开关、新依赖。

**但也绝不能「最小化掉」**：边界检查、超时、错误处理、CRC / 校验、Flash 对齐与寿命、
中断与任务的所有权、临界区、看门狗与复位原因、标定与容差。

### 未知项怎么办

- **能从仓库或权威文档查到的，不要问用户。**
- 查不到、且会改变方案 / 风险 / 验收的，才问。
- 一时确认不了的，明确标 `UNKNOWN`；**不要把假设当成硬件事实**。

### 每次收尾前走一遍

1. 行为、边界、并发、时序、硬件、恢复 —— 有没有漏
2. **最小性复查**：有没有重复代码、多余的抽象、没必要的依赖或文件
3. 搜一遍剩下的 `TODO` / 临时调试代码 / 没处理的调用点
4. 验收条件**逐条**对上证据
5. 更新受影响的文档（`PROJECT.md` 的能力地图 / `DEBUG.md`）
6. 说清**哪些没测、哪些还开着、什么时候会造成影响**

---

## 工程速查

- 工程说明、引脚表、中断分配、踩坑记录：见 `PROJECT.md`
- **排障手册（串口日志怎么读、故障决策树、已踩过的坑及根因）：见 `DEBUG.md`**
  —— 用户报「卡住 / 不工作 / 没反应」时，**先按 DEBUG.md 第 3 节让他提供串口日志**，
     不要靠猜。日志里 `[1] [2] [3]`、`HB`、`!!! HARDFAULT !!!` 各自代表什么，
     以及断电/复位后的四种日志表现分别对应什么结论，都写在里面了。
     启动日志和故障报告**永远打印**，不受 `DBG_UART` 开关影响。
- SysConfig 配置改完要重新构建，`Debug/ti_msp_dl_config.*` 会自动重新生成
  ★ 但**务必确认生成本身没报错**：SysConfig 失败时编译会中途停住，
    表现为「代码改了但板子上还是旧固件」。`gmake` 会自动调用匹配版本的 SysConfig。
- 改 `empty.syscfg` 的 `PWM3.timerCount` 时，记得同步 `hardware/motor.c` 的 `MOTOR_PWM_PERIOD`
- ⚠️ **不要用 PowerShell 的 `Get-Content` / `Set-Content` 改本仓库的文本文件！**
  PowerShell 5.1 的 `Get-Content` **默认按 ANSI(GBK) 解码**，会把 UTF-8 的中文
  读成乱码，`Set-Content` 再把乱码写回去 —— **整个文件的注释就毁了**。
  （实测踩过：`PROJECT.md` 被写坏过一次，只能 `git checkout` 重做）
  真要处理就用显式编码：
  `[IO.File]::ReadAllLines($p, [Text.Encoding]::UTF8)` +
  `[IO.File]::WriteAllLines($p, $lines, (New-Object Text.UTF8Encoding($false)))`；
  或者直接用编辑工具 —— **编辑工具是 UTF-8 安全的**。
