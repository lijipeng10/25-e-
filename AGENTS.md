# AGENTS.md —— 本工程开发约定

> 本文件给 AI 助手(以及人)看: 在这个仓库里干活必须遵守的规则。

---

## 注意事项

- **每次改动完成后，都必须创建一个对应的 Git commit**，以便后续追踪和回滚。
- **每次改动后，都必须编写或更新相关测试，并在交付给用户前，确保所有测试和验证全部通过。**

---

## 本工程怎么落地上面的两条

### 1. 每次改动都要 commit

- 一个逻辑改动 = 一个 commit，不要攒着一起提交。
- commit message 写清楚**改了什么、为什么改**；踩过的坑一并写进去（这类信息最值钱）。
- 提交前确认 `git status` 里没有误加入的构建产物 —— `Debug/` 已被 `.gitignore` 排除。

### 2. 每次改动都要测试/验证

本工程是嵌入式裸机工程，没有单元测试框架，所以"测试"分两层，**两层都要做**：

**(a) 编译验证（每次必做，可自动）**

```powershell
& 'D:/TI/ccs2025/ccs/utils/bin/gmake.exe' -C Debug -j4 all
```

验收标准：**exit code 0，且没有 error、没有 warning**。
注意：`gmake` 的报错会带 `[exit code: N]` 标记；只看"Finished building target"
不够，必须确认没有 warning。

**(b) 硬件验证（涉及外设行为时必须做）**

编译通过只能保证语法正确，**不能保证硬件行为正确**。
凡是改了外设初始化、时序、引脚、通信协议，交付前都要给出**可观察的验收现象**：

| 改了什么 | 需要的验收现象 |
| --- | --- |
| 显示(OLED) | 屏幕上出现预期画面，字符可读、边框完整 |
| 电机 | 按指定按键后电机按预期方向/速度转动 |
| 按键 | 串口或屏幕能看到正确的键码 |
| 通信(SPI/I2C/UART) | 对端能正确收到数据 |
| 传感器(灰度/陀螺仪) | 串口或屏幕能看到合理数值 |

如果无法自行烧录验证，**必须在交付时明确告诉用户**：
"编译已通过，但硬件行为需要你烧录确认，验收现象是 XXX"。
**不要把"能编译"当作"功能正确"交付。**

### 3. 交付时一并说明

- 改了哪些文件
- 验证做到了哪一层（编译 / 硬件）
- 还有哪些没验证、需要用户确认什么

---

## 工程速查

- 工程说明、引脚表、中断分配、踩坑记录：见 `PROJECT.md`
- **排障手册（串口日志怎么读、故障决策树、已踩过的坑及根因）：见 `DEBUG.md`**
  —— 用户报「卡住 / 不工作 / 没反应」时，**先按 DEBUG.md 第 3 节让他提供串口日志**，
     不要靠猜。日志里 `[1] [2] [3]`、`HB`、`!!! HARDFAULT !!!` 各自代表什么，
     以及断电/复位后的四种日志表现分别对应什么结论，都写在里面了。
     启动日志和故障报告**永远打印**，不受 `DBG_UART` 开关影响。
- SysConfig 配置改完要重新构建，`Debug/ti_msp_dl_config.*` 会自动重新生成
  ★ 但**务必确认生成本身没报错**：SysConfig 失败时编译会中途停住，
    表现为「代码改了但板子上还是旧固件」。`gmake` 会自动调用匹配版本的 SysConfig。
- 改 `empty.syscfg` 的 `PWM3.timerCount` 时，记得同步 `hardware/motor.c` 的 `MOTOR_PWM_PERIOD`
