/* ============================================================================
 *  line_follow.c —— 【已清空】
 * ----------------------------------------------------------------------------
 *  2026/09/15: 循迹那一套(双环航向 + 弯道原地转向 + 灰度处理)全删了。
 *
 *  为什么: 它和"编码器闭环测速"叠在一起, 出问题时根本分不清是哪一层。
 *  所以先把速度这一层单独测通, 再往上加。
 *
 *  ★ 完整实现都在 git 历史里, 没丢:
 *      git show 4ddf462:system/line_follow.c
 *      或者 git checkout <commit> -- system/line_follow.c
 *
 *  ★ 为什么文件留着而不删: 它在 CCS 生成的构建列表里
 *    (Debug/system/subdir_vars.mk), 直接删文件会编译不过。
 * ==========================================================================*/
#include "line_follow.h"

